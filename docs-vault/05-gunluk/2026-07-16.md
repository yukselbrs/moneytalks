# 2026-07-16 — Oturum Notu

## VİOP Nedir — implementasyon tamamlandı (akşam)
Scroll-hikaye 3 turda bitti: iskelet+B1-6 → SVG set-piece'leri (bardak/asansör/buğday) → simülasyon+nav+sitemap. Rect-tabanlı sahne aktivasyonu (IO pane'de ölü çıktı — kalıcı karar). Canlı doğrulama: sahneler, 4 soru kartı, simülasyonun çağrı yolu uçtan uca. Kalan: gerçek cihaz mobil turu + hukuki göz (Barış).

## Docs-vault kurulumu ve bekleyen manuel adımlar

**Kim:** Kaan (Claude ile)

## Bugün yapılanlar
- Kaan docs-vault'a ilk kez bağlandı: Obsidian'da `~/moneytalks/docs-vault` "Open folder as vault" ile açıldı.
- Kurulum sırasında yanlışlıkla oluşan `docs-vault/moneytalks/` boş alt-vault klasörü silindi (repo'ya commit'lenmemişti, iz yok).
- Claude Desktop MCP filesystem server'ının yolu `~/Documents/Obsidian Vault` → `~/moneytalks/docs-vault` olarak güncellendi; Claude artık vault'u doğrudan okuyup yazabiliyor.
- Not: Kaan'ın `~/Documents/Obsidian Vault` altında kurulum denemesinden kalan 5 deneme notu var; repo dışında, istenirse silinebilir.

## Bekleyen manuel adımlar (ADR'lerden derlendi)
Aşağıdakiler kod tarafı bitmiş ama canlıya alınması elle yapılacak işler. Kaynak ADR'ler parantezde.

### Supabase — SQL Editor'de çalıştırılacak (tek seferde koşulabilir)
- [ ] `supabase/migrations.sql` yeni bölümleri: `rate_limits` + `rate_limit_hit` RPC ([[track1-gorev3-rate-limit-supabase]])
- [ ] `profiles` kolon-seviyesi REVOKE/GRANT (email sızıntısı fix'i) ([[track1-gorev4-rls-audit]])
- [ ] KAP Tercümanı 3 tablosu: `kap_bildirimleri`, `kap_bildirim_gonderim`, `kap_cursor` ([[kap-tercumani-supabase-semasi]], [[track1-gorev9-kap-tercumani]])
- [ ] `karne_gonderim` tablosu ([[track1-gorev12-haftalik-karne]])
- Çalıştırılana kadar: rate limit fallback modda (cold start'ta sıfırlanıyor), KAP Tercümanı ve haftalık karne e-postaları fiilen çalışmıyor (hata üretmiyor, sessiz bekliyor).
- Migration sonrası: KAP cron'unu bir kez elle tetikleyip `{yeniBildirim, ozetlenen, epostaGonderilen}` sayaçlarını kontrol et.

### CRON_SECRET rotasyonu — Barış'ta ([[track1-gorev2-cron-secret-rotasyonu]])
- [ ] Vercel env güncelle (Sensitive) + redeploy + Cloudflare purge
- [ ] GitHub Actions secret güncelle (4 workflow aynı secret'ı okuyor)
- [ ] Curl ile doğrula (yeni → 200, eski → 401), sonra `.env.local`'deki rollback satırını sil

### Diğer
- [ ] Vercel dashboard → Cron Jobs boş mu kontrol et (çift alarm cron fix'i sonrası) ([[track1-gorev7-cift-cron]])
- [ ] MKK production API erişim başvurusu (şirket kurulumuyla birlikte) — demo'da takılı olan tek dış bağımlılık

## Açık sorular
- Deneme vault'undaki notlar silinsin mi? (Kaan karar verecek)
- Kaan'ın vault'ta hangi klasörleri aktif kullanacağı: 05-gunluk (oturum notları) + 02-urun (spec) doğal başlangıç.
